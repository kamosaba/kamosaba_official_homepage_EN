export const serverConfig = {
    // Display name
    name: "Kamosaba",

    // Connection address (used for API fetching)
    address: "play.kamosaba.net",

    // Display address (used for copy-pasting, etc.)
    displayAddress: "play.kamosaba.net",

    // Refresh interval (ms) - Default 60 seconds
    refreshInterval: 60000,

    // API endpoint (usually fine as is)
    apiBase: "https://api.mcsrvstat.us/3/"
};
